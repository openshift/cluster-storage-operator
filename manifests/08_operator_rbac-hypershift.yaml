# Deletion manifest for old cluster-admin binding — marked for deletion to avoid immutable roleRef conflict
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: cluster-storage-operator-role
  annotations:
    release.openshift.io/delete: "true"
---
# Replacement binding with fine-grained RBAC role
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  labels:
    addonmanager.kubernetes.io/mode: Reconcile
    kubernetes.io/cluster-service: "true"
  name: cluster-storage-operator-role
roleRef:
  kind: ClusterRole
  name: cluster-storage-operator-storage-role
subjects:
- kind: ServiceAccount
  name: cluster-storage-operator
  namespace: openshift-cluster-storage-operator
