# Shared Role for CSI driver operators to read Secrets from openshift-config-managed namespace
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: csi-driver-operator-config-managed-secrets-reader
  namespace: openshift-config-managed
rules:
  - apiGroups:
    - ''
    resources:
    - secrets
    verbs:
    - get
    - list
    - watch
